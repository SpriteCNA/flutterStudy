import 'package:flutter/material.dart';

class AppointScene extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: new AppBar(
          title: new Text('预约')
      ),
      body: new Container(
          child: new Text('预约')
      ),
    );
  }
}